name: Análise Estática de Código

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  static-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Executar análise estática com Maven
        # O -DskipTests evita que os testes de UI falhem por falta de servidor/Chrome
        run: mvn verify -DskipTests --file Sprint-RestAPI/pom.xml

      - name: Publicar relatórios
        uses: actions/upload-artifact@v4
        with:
          name: analise-estatica
          path: |
            Sprint-RestAPI/target/checkstyle-result.xml
            Sprint-RestAPI/target/spotbugsXml.xml